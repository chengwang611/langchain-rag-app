# Required for OpenAI embeddings + chat (default)
OPENAI_API_KEY=sk-...

# Optional overrides
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Paths (relative to project root)
DATA_DIR=data
CHROMA_DIR=.chroma
COLLECTION_NAME=rag_docs
